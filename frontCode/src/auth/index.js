// 统一管理理一下请求接口
import {
    // 树相关接口
    getAllMenu,
    addMenu,
    deleteMenu,
    queryMenuById,
    editMenu,
    // 角色
    roleMenuByMenuId,
    addRole,
    listRoles,
    deleteRole,
    editRole
} from './lss/index'

export default {
    getAllMenu,
    addMenu,
    deleteMenu,
    queryMenuById,
    editMenu,
    roleMenuByMenuId,
    addRole,
    listRoles,
    deleteRole,
    editRole
}